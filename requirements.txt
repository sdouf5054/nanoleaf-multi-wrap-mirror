# Core dependencies
numpy>=1.24
opencv-python>=4.8

# Qt framework
PySide6>=6.6

# USB HID (Windows only)
hidapi>=0.14

# Screen capture (Windows only — install manually)
# pip install dxcam

# Audio capture (Windows only)
# pip install PyAudioWPatch

# Utilities
keyboard>=0.13
Pillow>=10.0
psutil>=5.9

# 미디어 연동 (선택 — 앨범 아트 + PlaybackType 판별)
# pip install winrt-runtime winrt-Windows.Media.Control winrt-Windows.Storage.Streams winrt-Windows.Foundation winrt-Windows.Media