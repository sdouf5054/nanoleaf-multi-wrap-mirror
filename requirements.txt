# Nanoleaf Screen Mirror — Dependencies

# Core
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0

# Qt
PySide6>=6.6

# Screen capture
mss>=9.0
dxcam                          # DXGI Desktop Duplication (Windows, optional fallback)

# Audio capture (Windows WASAPI Loopback)
PyAudioWPatch

# USB HID
hidapi>=0.14

# System
keyboard>=0.13
psutil>=5.9

# Media session (Windows SMTC — album art + playback type)
winrt-runtime
winrt-Windows.Media.Control
winrt-Windows.Storage.Streams
winrt-Windows.Foundation
winrt-Windows.Media